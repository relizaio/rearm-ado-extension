{
    "name": "rearm-cli-ado-extension",
    "version": "1.1.1",
    "description": "Azure DevOps extension for ReARM CLI",
    "scripts": {
        "build": "cd tasks/RearmCliInstall && npm ci --ignore-scripts && npm run build && cd ../RearmReleaseInitialize && npm ci --ignore-scripts && npm run build && cd ../RearmReleaseFinalize && npm ci --ignore-scripts && npm run build",
        "package": "tfx extension create --manifest-globs vss-extension.json"
    },
    "devDependencies": {
        "tfx-cli": "^0.16.0"
    }
}
