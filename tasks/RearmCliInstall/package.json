{
    "name": "rearm-cli-install-task",
    "version": "0.4.4",
    "description": "Azure DevOps task to install Rearm CLI",
    "main": "index.js",
    "scripts": {
        "build": "tsc"
    },
    "dependencies": {
        "azure-pipelines-task-lib": "^4.11.0",
        "azure-pipelines-tool-lib": "^2.0.7"
    },
    "devDependencies": {
        "@types/node": "^20.10.0",
        "typescript": "^5.3.2"
    }
}
